{
  "name": "flashcards",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node --watch api.js",
    "test": "export NODE_ENV=test && (find . \\( -regex '.*.js$' -o -regex '.*.yaml$' -o -regex '.*.json' \\) -not -path './codegen/**' -not -path './node_modules/**' -not -path './sketches/**' | entr mocha tests/**)",
    "watch": "export NODE_ENV=test && mocha --watch tests/**"
  },
  "author": "Johannes Nam",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.10",
    "@types/express": "^4.17.21",
    "@types/mocha": "^10.0.4",
    "@types/node": "^20.9.0",
    "chai": "^4.3.10",
    "chai-http": "^4.4.0",
    "mocha": "^10.2.0"
  }
}
